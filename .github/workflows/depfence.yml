name: Dependency Security Scan

on:
  push:
    branches: [main, master]
    paths:
      - "**/package-lock.json"
      - "**/yarn.lock"
      - "**/pnpm-lock.yaml"
      - "**/requirements*.txt"
      - "**/poetry.lock"
      - "**/Cargo.lock"
      - "**/go.sum"
  pull_request:
    paths:
      - "**/package-lock.json"
      - "**/yarn.lock"
      - "**/pnpm-lock.yaml"
      - "**/requirements*.txt"
      - "**/poetry.lock"
      - "**/Cargo.lock"
      - "**/go.sum"
  schedule:
    # Weekly scan every Monday at 06:00 UTC
    - cron: "0 6 * * 1"
  workflow_dispatch:

jobs:
  depfence:
    name: depfence scan
    runs-on: ubuntu-latest

    permissions:
      # Required to upload SARIF to GitHub Code Scanning
      security-events: write
      # Required to check out private repos
      contents: read

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"

      - name: Install depfence
        run: pip install depfence

      - name: Run dependency security scan
        run: depfence scan . --format sarif -o results.sarif
        # Continue so the SARIF upload step always runs, even on findings
        continue-on-error: true

      - name: Upload SARIF to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@ff0a06e83cb2de871e5a09832bc6a81e7276941f # v3
        if: always()
        with:
          sarif_file: results.sarif
          category: depfence
